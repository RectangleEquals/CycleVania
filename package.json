{
  "name": "cyclevania",
  "private": true,
  "version": "0.1.0",
  "description": "CycleVania SDK workspace — deterministic procedural metroidvania level generation (Rust core + napi addon + WASM).",
  "scripts": {
    "build:addon": "cargo build -p cv-bindings",
    "smoke:addon": "node scripts/smoke-addon.cjs",
    "verify:addon": "cargo build -p cv-bindings && node scripts/smoke-addon.cjs",
    "build:wasm-probes": "cargo build -p cv-determinism --example determinism_probe --target wasm32-unknown-unknown && cargo build -p cv-core --example core_probe --target wasm32-unknown-unknown",
    "verify:wasm-golden": "npm run build:wasm-probes && node scripts/wasm-golden.cjs",
    "verify:determinism": "cargo test -p cv-determinism -p cv-core && npm run verify:wasm-golden",
    "verify": "cargo test && npm run verify:wasm-golden && npm run verify:addon"
  }
}
