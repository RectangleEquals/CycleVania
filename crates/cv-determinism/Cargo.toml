[package]
name = "cv-determinism"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Owned PRNG, owned math, and ordered containers — the determinism foundation."

[lints]
workspace = true

# The cross-target determinism probe, built for wasm32 and read back by scripts/wasm-golden.cjs.
[[example]]
name = "wasm_probe"
crate-type = ["cdylib"]

[dependencies]
