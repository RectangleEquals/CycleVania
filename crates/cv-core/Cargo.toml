[package]
name = "cv-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Data model, L0-L5 pipeline, solver, and the Context API (no I/O)."

[lints]
workspace = true

# The data model's cross-target determinism probe (see cv-determinism's for the naming note).
[[example]]
name = "core_probe"
crate-type = ["cdylib"]

[dependencies]
cv-determinism = { workspace = true }
cv-vm = { workspace = true }
