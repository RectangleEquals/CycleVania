{
  "name": "@cyclevania/inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CycleVania dataset workbench — per-layer synced views, real-geometry rendering, Play mode, data editor, seed lab, and reproduction bundles.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cyclevania/core": "workspace:*",
    "@cyclevania/examples": "workspace:*"
  },
  "devDependencies": {
    "three": "^0.170.0",
    "@types/three": "^0.170.0",
    "vite": "^6.0.0"
  }
}
