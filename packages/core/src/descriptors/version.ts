/**
 * CycleVania's own algorithm version. Any change to any generation algorithm that
 * alters output for the same inputs bumps this — so tooling can say *why* a world
 * changed (data vs. engine version) instead of leaving the host guessing.
 */
export const GENERATION_VERSION = "1.0.0";
